# Performance run 20260805T021709Z

| Field | Value |
|-------|-------|
| Overall | **FAIL** |
| Collected at (UTC) | 2026-08-05T02:22:06Z |
| BASE_URL | http://localhost:8080 |
| Accounts | 50 |
| Events / account | 40 (1 seed + 39 main) |
| Published (total) | 2000 |
| Publisher SDK window s / rate | 1.757s / 1109.7 msg/s (not gated) |
| Backlog DelaySeconds | 30 |
| Ingest consume window (backlog visible→drain) s | 3 |
| `ingestion.events` Δ (T1−T0b) | 1950 |
| **Ingest rate (events/s)** | **650.0** (target ≥ 2000) |
| Ingest gate | FAIL |
| **Server http.server.requests p95 (ms)** | **14.20** (target ≤ 100) |
| **Server http.server.requests p99 (ms)** | **21.75** (target ≤ 250) |
| Query SLO gate (server-side) | PASS |
| k6 concurrent query | PASS (log: `deploy/perf/work/20260805T021709Z/k6.log`) |
| Correctness + durability | PASS |

## What this run proves

1. Query load only accepted **HTTP 200** with a balance body (404 does not pass).
2. Query SLO uses **server-side** Micrometer `http.server.requests` histogram quantiles for `GET /balances/{accountId}` (not k6 client RTT alone).
3. Ingest throughput is **consume-only**: main load is published with SQS `DelaySeconds`; the gate waits until `publish_end + DelaySeconds` (and delayed=0), then uses `Δ ingestion.events / (visible backlog → drain)`. Drain counts delayed messages. Publisher msg/s is separate.
4. Final snapshot amounts match the workload oracle; queue drained; durable row counts ≥ published.
5. k6 runs **after** ingest drain against final balances to validate query SLO under load.

## Artifacts

- Work dir: `deploy/perf/work/20260805T021709Z/`
- Metrics: `metrics-t0.txt`, `metrics-t0b.txt`, `metrics-t1.txt`, `metrics-t2.txt`, prometheus scrapes
- Correctness: `correctness.txt`
- k6: `k6.log`

## Notes

- LocalStack / laptop results are **reference** numbers; production-class hardware may differ.
- If server percentiles show `nan`, confirm `/actuator/prometheus` returns histogram buckets for `http_server_requests_seconds`.
- Raise consumer capacity for higher EPS: `SQS_RECEIVER_COUNT`, `SQS_MAX_CONCURRENT`, `DB_POOL_SIZE` (restart the app).
- Keep `INGEST_BACKLOG_DELAY_SECONDS` above SDK publish duration so staging finishes before visibility.
