# Performance run 20260805T141039Z

| Field | Value |
|-------|-------|
| Overall | **FAIL** |
| Collected at (UTC) | 2026-08-05T14:18:04Z |
| BASE_URL | http://localhost:8080 |
| Accounts | 50 |
| Events / account | 40 (1 seed + 39 main) |
| Published (total) | 2000 |
| Publisher SDK window s / rate | 2.457s / 793.5 msg/s (not gated) |
| Backlog DelaySeconds | 30 |
| Wall consume window (visible→drain) s | 12 |
| `ingestion.events` Δ (T1−T0b) | 1950 |
| Wall ingest rate (events/s) | 162.5 |
| DB `first_processed_at` count / span_s / eps | 0 / nan / nan |
| **Ingest rate used for gate (events/s)** | **162.5** (source=wall_drain; target ≥ 2000) |
| Ingest gate | FAIL |
| **Server http.server.requests p95 @T1 (ms)** | **109.05** (target ≤ 100; k6 overlapped ingest) |
| **Server http.server.requests p99 @T1 (ms)** | **257.25** (target ≤ 250; k6 overlapped ingest) |
| Query SLO gate (SC-003 scrape = T1) | FAIL |
| k6 (started before drain) | FAIL (log: `deploy/perf/work/20260805T141039Z/k6.log`) |
| Correctness + durability | PASS |

## What this run is (and is not)

1. Query checks require **HTTP 200** + balance body (404 fails).
2. SC-003 query scrape is **T1** (immediately after drain) while k6 was running during consume. Histograms are **cumulative** (include seed traffic); T2 after k6 finishes is observational only.
3. Ingest rate prefers durable `processed_transaction.first_processed_at` min/max after `consume_since`; falls back to wall drain. Publisher msg/s is not the gate.
4. A short local window (few seconds, one process, LocalStack) is a **diagnostic observation**, not production sizing evidence. Do **not** multiply EPS × replica count.
5. Proving ≥2k EPS + SC-003 requires: multi-pod EKS/RDS, simultaneous ingest+query, **10–15 min** sustained load, spec account/event distribution, plus backlog-drain / DB saturation / failure-recovery drills.

## Artifacts

- Work dir: `deploy/perf/work/20260805T141039Z/`
- Metrics: `metrics-t0.txt`, `metrics-t0b.txt`, `metrics-t1.txt`, `metrics-t2.txt`, prometheus scrapes
- DB span: `db_ingest_span.txt`
- Correctness: `correctness.txt`
- k6: `k6.log`

## Notes

- LocalStack / laptop results are **diagnostics**.
- If DB span is `nan`, install `psql` against localhost:5432 or run compose postgres; gate then uses wall drain only.
- Keep `INGEST_BACKLOG_DELAY_SECONDS` above SDK publish duration so staging finishes before visibility.
